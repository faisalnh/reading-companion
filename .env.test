# Test Environment for Database Setup
# This file is used for testing PostgreSQL initialization only

# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_PORT=3000

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=reading_buddy
DB_USER=reading_buddy
DB_PASSWORD=test-password-123

# NextAuth
NEXTAUTH_SECRET=test-secret-for-database-initialization-only
NEXTAUTH_URL=http://localhost:3000

# MinIO
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=test-minio-123
MINIO_BUCKET_NAME=reading-buddy

# Deployment mode
DEPLOYMENT_MODE=selfhosted
